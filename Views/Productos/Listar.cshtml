@{
    ViewData["Title"] = "Productos";
}

@model List<Producto>

<a asp-action="Crear" class="text-decoration-none">Crear nuevo producto</a>

<table class="table mt-3 table-hover">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Producto</th>
            <th scope="col">Precio</th>
        </tr>
    </thead>
    <tbody>
        @foreach(Producto producto in Model)
{

        <tr>
            <td>@producto.Id</td>
            <td>@producto.Descripcion</td>
            <td>@producto.Precio</td>
            <td><a asp-action="Modificar" asp-route-idProd="@producto.Id" class="text-decoration-none">Modificar</a></td>
            <td><a asp-action="Eliminar" asp-route-idProd="@producto.Id" class="text-decoration-none">Eliminar</a></td>
        </tr>

}
    </tbody>
</table>
